const Card = ({ children }) => {
  return <div>{children}</div>;
};

export default Card;

// const Card = (props) => {
//   console.log(props);
//   return <div>{props.children}</div>;
// };

// export default Card;
