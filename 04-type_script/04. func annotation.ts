// Func parameters Annotation
function addone(num: number) {
  return num + 1;
}

const res = addone(3);
console.log(res);

// arrow func
const add = (x: number) => x + 1;
console.log(add(2));

// const add = (x: number) => x + 1;
// console.log(add(2,4));
